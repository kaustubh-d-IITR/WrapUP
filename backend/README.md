# WrapUP backend (stub)

Minimal Express stub. Not required to run the frontend.

## Endpoints
- `GET  /health` → `{ ok: true }`
- `POST /generate-trip` → mock trip JSON

## Run
```bash
cd backend
npm install
npm start   # serves on http://localhost:8787
```
